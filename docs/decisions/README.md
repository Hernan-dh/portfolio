# Decisions

This directory contains lasting technical or functional decisions. ADRs remain in sequence even after they are superseded.

Valid states: `proposed`, `accepted`, `superseded`, and `rejected`.

```bash
python scripts/document.py decision "Decision title"
```

Do not create ADRs for cosmetic changes, trivial fixes, or refactors without behavioral changes.
